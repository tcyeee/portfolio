---
const { tag } = Astro.props;
---

<div class="badge badge-info p-2">
  {tag === 'WEB' && <span class="icon-web" />}
  {tag === 'MOBILE' && <span class="icon-mobile" />}
  <span class="text-gray-100 font-bold text-[13px] mt-1 ml-[-4px]">{tag}</span>
</div>
