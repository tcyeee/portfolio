---
import TechItem from './TechItem.astro';
const { tags = [] } = Astro.props;
---

<div class="flex flex-wrap items-center gap-3">
  {tags.map((skill: string) => <TechItem size="50" name={skill} />)}
</div>
